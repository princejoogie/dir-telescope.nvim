# dir-telescope.nvim

grep or find files in selected directory with telescope

## Usage

- packer
```lua
	use({
		"dir-telescope.nvim",
		config = function()
			require("dir-telescope").setup()
		end,
	})

```
